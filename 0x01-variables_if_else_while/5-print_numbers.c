#include <stdio.h>

/**
 * main - Function Entry point
 * Return: 0 to show code executed Successfully
 */

int main(void)
{
int a;

for (a = 0; a < 10; a++)
printf("%d", a);
putchar('\n');
return (0);
}
